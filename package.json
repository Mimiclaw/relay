{
  "name": "mimiclaw-ws",
  "version": "0.1.1",
  "private": false,
  "description": "Mimiclaw workforce websocket service (single-file Node.js/TypeScript, pkg-ready).",
  "main": "dist/server.js",
  "bin": {
    "mimiclaw-ws": "dist/server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true});require('fs').rmSync('release',{recursive:true,force:true});\"",
    "typecheck": "tsc server.ts --module commonjs --target es2020 --esModuleInterop --moduleResolution node --skipLibCheck --noEmit",
    "build": "npm run clean && tsc server.ts --module commonjs --target es2020 --esModuleInterop --moduleResolution node --skipLibCheck --outDir dist",
    "start": "node dist/server.js",
    "build:pkg": "npm run build && pkg dist/server.js --targets node20-win-x64,node20-linux-x64,node20-macos-x64 --out-path release",
    "prepack": "npm run build",
    "postinstall": "node -e \"const fs=require('fs');const os=require('os');const path=require('path');const dir=path.join(os.homedir(),'.mimiclaw-relay');fs.mkdirSync(dir,{recursive:true});console.log('[mimiclaw-ws] relay dir ready:',dir);\""
  },
  "dependencies": {
    "@seald-io/nedb": "^4.1.2",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "@types/ws": "^8.18.1",
    "pkg": "^5.8.1",
    "typescript": "^5.9.2"
  }
}
